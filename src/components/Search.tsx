import { TextField } from "@mui/material";

// Serach component for elastic requests
const Search = () => {
    return (
        <TextField id={"standard-basic"} label={"Search"} variant={"standard"} />
    )
}

export default Search